import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { Button, PageHeader } from '../../components/common'

const slots = [['Sep 10, 2026','2:00 - 3:00 PM','96%'],['Sep 10, 2026','4:00 - 5:00 PM','92%'],['Sep 11, 2026','2:00 - 3:00 PM','89%'],['Sep 12, 2026','11:00 AM - 12:00 PM','85%']]
export function SlotRecommendationsPage() { const navigate = useNavigate(); const [selected, setSelected] = useState(0); return <><div className="back-link"><Link to="/ta/candidates/candidate-1/setup-interview">← Back to setup</Link></div><PageHeader eyebrow="Interview workflow · Step 2 of 2" title="Recommended interview slots" description="We found the best times based on everyone's availability."/><div className="slot-layout"><div className="slot-main"><section className="best-match"><div className="best-badge">✦ BEST MATCH</div><div className="best-content"><div><div className="eyebrow">Wednesday</div><h2>September 10, 2026</h2><strong>2:00 PM <span>to</span> 3:00 PM <em>IST</em></strong></div><div className="best-score"><span>Scheduling score</span><strong>96<small>%</small></strong><div className="progress light"><i style={{ width:'96%' }}/></div></div></div><div className="availability-row">{['Rahul Sharma','Rahul Mehta','Priya Shah'].map((name,index) => <div className="availability" key={name}><span className="status-dot green"/><div><small>{index ? 'Interviewer' : 'Candidate'}</small><strong>{name}</strong></div><b>Available</b></div>)}</div></section><section className="panel"><div className="panel-head"><div><h2>Alternative times</h2><p>Other compatible slots ranked by scheduling score</p></div></div><div className="slot-list">{slots.map((slot,index) => <button className={`slot-row ${selected === index ? 'selected' : ''}`} key={slot[0] + slot[1]} onClick={() => setSelected(index)}><span className="radio">{selected === index && '✓'}</span><span><strong>{slot[0]}</strong><small>{slot[1]} IST</small></span><b>{slot[2]} <small>match</small></b><span>→</span></button>)}</div></section></div><aside className="slot-aside"><section className="panel why-panel"><div className="section-kicker">WHY THIS SLOT?</div><h2>Everything lines up</h2>{['Everyone is available','Within preferred date range','Within working hours','No calendar conflicts','High compatibility'].map(item => <div className="why-item" key={item}><span>✓</span>{item}</div>)}<div className="slot-summary"><span>Selected slot</span><strong>{slots[selected][0]}</strong><b>{slots[selected][1]} IST</b></div><Button onClick={() => navigate('/interviewer/dashboard')} icon="→">Send slot to interviewer</Button></section></aside></div></> }
