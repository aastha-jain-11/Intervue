import { Link } from 'react-router-dom'
import { candidates } from '../../data/demoData'
import { Button, CandidateTable, PageHeader, SearchControls } from '../../components/common'

export function TADashboardPage() { return <><PageHeader eyebrow="Monday, September 7, 2026" title="Good morning, Aarav" description="Your hiring workspace at a glance." action={<Button to="/ta/interviews/create" icon="+">Create interview</Button>} /><div className="metric-grid">{[['Total candidates','124','+12.5%','indigo'],['Screened','98','+8.2%','blue'],['Shortlisted','42','+4.8%','green'],['Pending TA action','18','Needs attention','orange'],['Interviews scheduled','26','+18.4%','purple']].map(([label,value,change,color]) => <div className="metric-card" key={label}><div className={`metric-icon ${color}`}>◈</div><div className="metric-label">{label}<span className={change === 'Needs attention' ? 'negative' : 'positive'}>{change}</span></div><strong>{value}</strong><span className="metric-foot">vs last 30 days</span></div>)}</div><div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><h2>Candidate shortlist</h2><p>AI-ranked candidates ready for your review</p></div><Link className="text-link" to="/ta/candidates">View all →</Link></div><SearchControls /><CandidateTable candidates={candidates} compact /></section><section className="panel pipeline-panel"><div className="panel-head"><div><h2>Hiring pipeline</h2><p>Current candidate progression</p></div></div><div className="pipeline-total"><strong>124</strong><span>total candidates</span></div>{[['Applied','54','indigo'],['Screened','38','blue'],['Shortlisted','20','green'],['Interview','8','orange'],['Offer','4','purple']].map(([label,count,color]) => <div className="pipeline-row" key={label}><span className={`pipeline-dot ${color}`} /><span>{label}</span><div className="pipeline-bar"><i style={{ width: `${Number(count) * 1.35}%` }} /></div><b>{count}</b></div>)}<div className="pipeline-footer"><span className="status-dot green" />Pipeline health<strong>Good</strong></div></section></div></> }
