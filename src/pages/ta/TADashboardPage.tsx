import { Link } from 'react-router-dom'
import { useAuth } from '../../auth/AuthContext'
import { Button, PageHeader } from '../../components/common'

export function TADashboardPage() {
  const { user } = useAuth()
  return <><PageHeader eyebrow="Talent acquisition" title={`Welcome back, ${user?.name.split(' ')[0] || 'there'}`} description="Manage applications and schedule interviews from live backend records." action={<Button to="/ta/interviews/create" icon="+">Create interview</Button>} /><div className="metric-grid"><div className="metric-card"><div className="metric-label">Authenticated profile</div><strong>{user?.name || 'Unavailable'}</strong><span className="metric-foot">{user?.email}</span></div><div className="metric-card"><div className="metric-label">Role</div><strong>{user?.role || 'Unavailable'}</strong><span className="metric-foot">From /users/me</span></div><div className="metric-card"><div className="metric-label">Applications</div><strong>View</strong><span className="metric-foot">Live records on the candidates page</span></div><div className="metric-card"><div className="metric-label">Interviews</div><strong>Manage</strong><span className="metric-foot">Live records on the interviews page</span></div></div><section className="panel"><div className="panel-head"><div><h2>Hiring workspace</h2><p>Use live applications to create interviews and trigger automatic scheduling.</p></div><Link className="text-link" to="/ta/interviews">View interviews →</Link></div><p>Dashboard analytics are not available from the current API, so no placeholder counts are shown.</p></section></>
}
